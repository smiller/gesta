/* No preprocessor: Svelte 5's compiler reads the erasable TypeScript a
   `<script lang="ts">` holds, and this repository's tsconfig already
   confines it to that (erasableSyntaxOnly). The file exists so the plugin
   stops announcing its absence on every run. */
export default {};
